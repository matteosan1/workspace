/*
#include "imageitem.h"
//Added by qt3to4:
#include <QPixmap>

ImageItem::ImageItem(QPixmap p, Q3Canvas *c)
 : Q3CanvasRectangle(c), pixmap(p) {
    setSize(pixmap.width(), pixmap.height());
}

ImageItem::~ImageItem()
{}

void ImageItem::drawShape(QPainter &p) {
    p.drawPixmap(int(x()), int(y()), pixmap);
}
*/
